next-version: 0.1.8
branches:
  pull-request:
    regex: (pull|pull\-requests|pr)[/-]
    mode: ContinuousDeployment
    tag: pr
  master: 
    mode: ContinuousDeployment
    tag: ''
  feature:
    regex: (!master)
    mode: ContinuousDeployment
    tag: pr
ignore:
  sha: []
