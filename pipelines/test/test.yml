parameters:
  scenarios: ["test_1.yml", "plan_with_comand_options_var_file.yml", "init_with_self_configured_backend.yml"]

stages:
- stage: test
  dependsOn: install
  variables:
    - group: env_test
  jobs:
    - ${{ each scenario in parameters.scenarios }}:
      - template: ${{ scenario }}

    